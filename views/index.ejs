<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <h1><%= title %></h1>
  <div class="gameCard-container">
    <% games.forEach(game => { %>
      
    <div class="gameCard">
    <img src="<%=game.cover_image_url%>" alt="<%=game.cover_image_url%>">
    <p><%= game.title %></p>
    </div>
    


    <% }) %>
  </div>
</body>
</html>